type: File Generation
desc: Generates a file with random data.

input_parameters:
  content_length:
    type: int
    unit: n/a
    value: 10
    min: 0
    desc: How many characters to generate in the file.
